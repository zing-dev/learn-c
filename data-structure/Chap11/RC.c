/*=============================================================
// �� �� ����RC
// ��������������һ����Բ���ֵ��˻���ʽ��ֵ
// ���������x ��ֵ���Ա���
//           y ��ֵ���Ա���
// �� �� ֵ����һ����Բ���ֵ��˻���ʽ��ֵ
//==============================================================*/
#include "math.h"

double RC(x, y)

double x, y;
{
double ll, t, t1, f, eps;
eps = 1.0e-30;
if(x<0||(x+
fabs(y)
)<eps)                                  /* ����Ա�����Χ*/
{
printf("Parameters incorrect\n");
return(0.0);
}
f = 1.0;
if(y<0.0)
{
f = sqrt(x) / sqrt(x - y);
x = x - y;
y = -y;
}
do{
ll = 2.0 * sqrt(x) * sqrt(y) + y;
x = (x + ll) / 4.0;                                        /* ����x,y*/
y = (y + ll) / 4.0;
t = (x + y + z) / 3.0;
t1 = 1.0 - y / t;
}while(
fabs(t1)
> 0.0012);                                 /* �����ֵ�����ж�x��y��ֽӽ�*/
t = (1.0 + t1 * t1 * (0.3 + t1 * (1.0 / 7.0 + t1 * (0.375 + 9.0 * t1 / 22.0)))) / sqrt(t);     /* ��̩��չ���������ֵ*/
t = f * t;                                                    /* ����ϵ��*/
return(t);
}

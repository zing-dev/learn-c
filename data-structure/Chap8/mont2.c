#include "math.h"
#include "stdlib.h"

/*======================================================
// ��������mont2
// �������������ؿ��޷�����ػ���
// ���������x0 x����������ޣ�x1 x�����������,
//           y0 y����������ޣ�y1 y�����������,
//           f ָ�򱻻�������ָ��
// ����ֵ��  ���ֽ���ֵ
=========================================================*/
double mont2(x0, x1, y0, y1, f)

double x0, x1, y0, y1;

double (*f)();

{
int k, n;
double z, p, x, y;
unsigned long r;            /* ���������*/
n = 1000;                  /* ����1000�������*/
z = 0.0;
for(
k = 0;
k<n;
k++)
{
r = rand();               /* ���������*/
p = (double) r / RAND_MAX;
x = x0 + (x1 - x0) * p;
r = rand();
p = (double) r / RAND_MAX;
y = y0 + (y1 - y0) * p;
z = z + (*f)(x, y) / n;          /* ���*/
}
z = z * (y1 - y0) * (x1 - x0);
return(z);
}
